#include "main.h"

/**
 * set_string - sets a value of a pointer to a char
 * @s: the double string pointer
 * @to: the single pointer
 * Return: Nothing
 */
void set_string(char **s, char *to)
{
	*s = to;
}
