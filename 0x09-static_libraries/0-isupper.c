#include "main.h"

/**
 ** _isupper - checks if alphabet is an uppercase
 ** @c: the character to be checked
 ** Return: 1 if upper and 0 if not
 **/
int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);

}
