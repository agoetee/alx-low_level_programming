#include "main.h"

/**
 * _isdigit - checks if int is between 0 and 9
 * @c: int to be checked
 * Return: 1 if is digit and 0 if not
 *
 */
int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
